# Wattplot — test / dev dependencies
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Runtime deps live in requirements.txt. These are only needed to run
# the test suite (`pytest firmware/tests/`) and the linter.

pytest>=8.0
ruff>=0.16.2          # lint (CI pins its own version)

# Optional: ESPHome itself. The firmware tests that compile/validate the
# YAML are skipped (via the `requires_esphome` marker in conftest.py) when
# it isn't installed, so this is not required for a normal test run.
# esphome>=2024.6.0
